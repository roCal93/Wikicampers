{# templates/disponibilite/new.html.twig et edit.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Créer/Modifier Disponibilité{% endblock %}

{% block body %}
<h1>{{ 'Créer'|default('Modifier') }} Disponibilité</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">{{ 'Créer'|default('Modifier') }}</button>
{{ form_end(form) }}

<a href="{{ path('disponibilite_index') }}">Retour à la liste</a>
{% endblock %}
